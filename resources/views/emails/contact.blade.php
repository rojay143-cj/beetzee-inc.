<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ $subject }}</title>
</head>
<body>
    <h4>{{ $subject }}</h4>
    <p>Dear {{ $Mailname }}</p>
    <p>{{ $Mailorganization }}</p>
    <p>{{ $Mailemail }}</p>
    <p>{{ $Mailnumber }}</p>
    <p>{{ $Mailmessage }}</p>
</body>
</html>
